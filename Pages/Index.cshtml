@page
@model IndexModel
@{
    ViewData["Title"] = "INÍCIO";
}

<div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-indicators">
        @for (int i = 0; i < Model.Slides.Count; i++)
        {
            <button type="button"
                    data-bs-target="#carouselExampleIndicators"
                    data-bs-slide-to="@i"
                    class="@(i == 0 ? "active" : "")"
                    aria-current="@(i == 0 ? "true" : "false")"
                    aria-label="Slide @(i + 1)">
            </button>
        }
    </div>

    <div class="carousel-inner">
        @for (int i = 0; i < Model.Slides.Count; i++)
        {
            var slide = Model.Slides[i];
            <div class="carousel-item @(i == 0 ? "active" : "")">
                <img src="@slide.ImagePath" class="d-block w-100" alt="@slide.Title">
                <div class="carousel-caption">
                    <h1 class="carousel-title">@slide.Title</h1>
                    <p>@Html.Raw(slide.Description)</p>
                    <a asp-page="@slide.Link" class="btn">Saiba mais</a>
                </div>
            </div>
        }
    </div>

    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Anterior</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Próximo</span>
    </button>
</div>

<div class="index-div">
    <h1 class="index-title">PROGRAMAÇÕES SEMANAIS</h1>

    <div class="cards-container">
        <div class="card-item">
            <h2>QUARTAS-FEIRAS</h2>
            <p>Estudo Bíblico</p>
            <p>19h30</p>
        </div>
        <div class="card-item">
            <h2>SEXTAS-FEIRAS</h2>
            <p>Culto de Oração</p>
            <p>19h30</p>
        </div>
        <div class="card-item">
            <h2>DOMINGOS</h2>
            <p>Escola Bíblica</p>
            <p>09h00</p>
        </div>
        <div class="card-item">
            <h2>DOMINGOS</h2>
            <p>Culto Principal: Louvor e Adoração</p>
            <p>19h30</p>
        </div>
    </div>
</div>

<div class="info-section">
    <div class="info-logo">
        <img src="~/images/logo_compacta.png" alt="Logo da Igreja">
    </div>

    <div class="info-location">
        <h4>LOCALIZAÇÃO</h4>
        <h5>Igreja Cristã Evangélica de Posse</h5>
        <p>Rua Benevenuto Barbosa de Souza, 970<br /> St. Santa Luzia</p>
        <p>Posse, Goiás</p>
    </div>

    <div class="info-icons">
        <a href="https://www.instagram.com/iceposse/" target="_blank">
            <img src="~/images/instagram.png" alt="Instagram">
        </a>
        <a href="https://maps.app.goo.gl/AR39QrRhayGYcC2C6" target="_blank">
            <img src="~/images/maps.png" alt="Google Maps">
        </a>
    </div>
</div>

